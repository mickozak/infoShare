(function () {
    var buttonElement = document.querySelector('button')

    buttonElement.addEventListener(
        'click',
        function (event) {
            console.log(event)
        }
    )

})()
